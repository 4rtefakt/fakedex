name = "fakedex"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

# Shared-dex database (tables auto-create on first request — no schema step).
# Local dev overrides this with a throwaway DB via the --d1 CLI flag.
[[d1_databases]]
binding = "DB"
database_name = "fakedex-db"
database_id = "65bd00b7-6c23-4166-a5c5-292876566ba6"
